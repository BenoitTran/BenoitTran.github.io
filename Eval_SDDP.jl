function Eval_SDDP(t, F_down, state)
"""
Input:
t, a time step.
F_down, an array of length T, which contains at each
component a matrix.
state, a vector.

Output:
The evaluation of the infima of quad. contained in F_down at the given state,
AFTER "de-homogeneization".
"""
end
