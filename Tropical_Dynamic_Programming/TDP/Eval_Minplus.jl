function Eval_Minplus(t, F_up, state)
"""
Input:
t, a time step
F_up, an array of length T, which contains at each
component a slope and a value at the origin.
state, a vector.

Output:
The evaluation of the supremum of the cuts contained in F_up at the given state.
"""

end
